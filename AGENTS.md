# Agent Instructions

- Always bump the `APP_VERSION` constant near the top of `Código.js` whenever you modify application code in this repository.
- Mention the updated version in your summary when touching `Código.js`.
